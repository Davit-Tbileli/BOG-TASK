================================================================================
                    BOG OFFERS CHATBOT - PROJECT DOCUMENTATION
================================================================================

პროექტის სახელი: Bank of Georgia Offers Chatbot
ტექნოლოგიები: Python, RAG, Qdrant, Sentence Transformers, Streamlit, LLM (OpenAI/Gemini)

================================================================================
                              პროექტის მიმოხილვა
================================================================================

ეს პროექტი წარმოადგენს სრულფასოვან RAG (Retrieval-Augmented Generation) სისტემას,
რომელიც საშუალებას აძლევს მომხმარებლებს მოძებნონ და მიიღონ ინფორმაცია 
Bank of Georgia-ს შეთავაზებების შესახებ ქართულ ენაზე.

სისტემა შედგება 4 ძირითადი კომპონენტისგან:
1. Web Scraper - მონაცემების შეგროვება bog.ge-დან
2. RAG System - ვექტორული ძიება და რეტრივალი
3. LLM Integration - ენის მოდელთან ინტეგრაცია
4. Streamlit UI - მომხმარებლის ინტერფეისი


================================================================================
                           I. WEB SCRAPER (scraper/)
================================================================================

მიზანი: bog.ge საიტიდან შეთავაზებების ავტომატური შეგროვება

ფაილები:
---------

1. playwright_scrape.py
   ----------------------
   - იყენებს Playwright ბიბლიოთეკას (Selenium-ის მოდერნული ალტერნატივა)
   - ამუშავებს დინამიურ კონტენტს (JavaScript lazy loading)
   - პროცესი:
     a) საიტზე შესვლა ინკოგნიტო რეჟიმში
     b) Cookie-ების დადასტურება
     c) გვერდის ბოლომდე სქროლი (lazy loading-ის გააქტიურება)
     d) Pagination-ის დამუშავება (bog.ge...currentPage{X})
     e) თითოეული გვერდის სქრეიპინგი

2. detail_workers.py
   ------------------
   - პარალელური სქრეიპინგი დეტალური ინფორმაციისთვის
   - 4 worker-ი ერთდროულად ამუშავებს სხვადასხვა შეთავაზებას
   - აღწერის ტექსტი მხოლოდ შეთავაზების დეტალურ გვერდზეა
   - Headless=False - ვიზუალური მონიტორინგისთვის

3. city_scrape.py
   ---------------
   - ქალაქების და მათი ID-ების შეგროვება
   - bog.ge იყენებს cityId პარამეტრს (მაგ: თბილისი=37)
   - ქალაქების ბაზის ავტომატური განახლება
   - აქაც პარალელური სქრეიპინგი (4 ვორქერი)

4. append_cities.py
   -----------------
   - თითოეული ქალაქის შეთავაზებების დამატება
   - ქალაქის ინფორმაციის მიბმა შესაბამის offer-ებთან


მონაცემთა ნაკადი:
-----------------
bog.ge → playwright_scrape.py → data/raw/found_offers.json
                                       ↓
                              detail_workers.py
                                       ↓
                              city_scrape.py → data/raw/cities.json
                                       ↓
                              append_cities.py
                                       ↓
                              data/processed/found_offers.json


================================================================================
                           II. RAG SYSTEM (rag/)
================================================================================

მიზანი: ვექტორული ძიების სისტემა შეთავაზებების ეფექტური მოძიებისთვის

ფაილები:
---------

1. embeddings.py - ემბედინგების გენერატორი
   -----------------------------------------
   
   კლასი: EmbeddingGenerator
   
   მოდელი: BAAI/bge-m3
   - მულტილინგვალური (ქართულს კარგად უჭერს მხარს)
   - 1024-განზომილებიანი ვექტორები
   
   ძირითადი მეთოდები:
   
   a) __init__(model_name, normalize_embeddings, device)
      - მოდელის ჩატვირთვა
      - normalize_embeddings=True (Cosine Similarity-სთვის აუცილებელი)
   
   b) generate_embeddings(texts) -> List[List[float]]
      - ტექსტების ვექტორებად გარდაქმნა
      - SentenceTransformer-ის encode() მეთოდი
   
   c) preprocess_offer(offer) -> str
      - შეთავაზების ტექსტური რეპრეზენტაცია
      - ქართული ლეიბლები: "ბრენდი:", "კატეგორია:", "სათაური:" და ა.შ.
      - _safe_str() - None/list/tuple უსაფრთხო კონვერტაცია
   
   d) embedding_dimension() -> int
      - ვექტორის ზომა (1024 bge-m3-სთვის)


2. vector_store.py - Qdrant ვექტორული მაღაზია
   -------------------------------------------
   
   კლასი: VectorStore
   
   Qdrant Cloud:
   - ღრუბლოვანი სისტემა (არ ტვირთავს ლოკალურ რესურსებს)
   - COSINE distance metric
   
   ძირითადი მეთოდები:
   
   a) __init__(config)
      - Qdrant კონფიგურაცია (URL, API key, collection name)
      - Lazy client initialization
   
   b) client (property)
      - QdrantClient-ის lazy initialization
      - timeout და connection settings
   
   c) create_collection(collection_name, vector_size)
      - კოლექციის შექმნა თუ არ არსებობს
      - VectorParams: size=1024, distance=COSINE
   
   d) add_documents(documents, embeddings, metadata)
      - PointStruct-ების შექმნა
      - Batch upsert (64 ერთეულიანი batch-ები)
      - _ensure_serializable_payload() - JSON თავსებადობა
   
   e) similarity_search(query_embedding, top_k) -> List[Tuple[payload, score]]
      - ვექტორული მსგავსების ძიება
      - top_k საუკეთესო შედეგის დაბრუნება


3. retriever.py - შედეგების მოძიება და რანჟირება
   ----------------------------------------------
   
   კლასი: OfferRetriever
   
   ძირითადი მეთოდები:
   
   a) __init__(embedding_generator, vector_store, config)
      - კომპონენტების ინიციალიზაცია
      - კონფიგურაცია: top_k, min_similarity_score, lexical_boost
   
   b) retrieve(query) -> List[Dict]
      - query-ს ემბედინგად გარდაქმნა
      - ვექტორული ძიება
      - rerank_results() გამოძახება
   
   c) rerank_results(query, results) -> List[Dict]
      - ლექსიკური გადაფარვის გამოთვლა (lexical overlap)
      - კომბინირებული score = vector_score + (lexical_boost * overlap)
      - Query ტიპის დეტექცია:
        * _is_recommendation_query() - "მირჩიე", "რეკომენდ"
        * _is_factual_question() - "ვინ", "როდის", "სად", "?"
      - ფაქტობრივი კითხვებისთვის: მინიმალური overlap მოთხოვნა
   
   დამხმარე ფუნქციები:
   - _tokenize(text) - ქართული და ლათინური ტოკენიზაცია
   - _is_recommendation_query(query) - რეკომენდაციის მოთხოვნის დეტექცია
   - _is_factual_question(query) - ფაქტობრივი კითხვის დეტექცია


4. taxonomy.py - ტაქსონომიის ძრავა
   --------------------------------
   
   კლასი: TaxonomyEngine
   
   მიზანი: შეთავაზების სარგებლის ნორმალიზაცია (ფასდაკლება/ქეშბექი/ქულები)
   
   taxonomy.json სტრუქტურა:
   - benefit_types: სარგებლის ტიპები (DISCOUNT_PERCENT, CASHBACK_PERCENT...)
   - rules: წესები pattern matching-ისთვის
   - offer_annotations: კონკრეტული შეთავაზებების override-ები
   
   ძირითადი მეთოდები:
   
   a) __init__(taxonomy_path)
      - taxonomy.json ჩატვირთვა
      - წესების კომპილაცია (regex patterns)
   
   b) normalize_offer(offer) -> NormalizedBenefit
      პრიორიტეტი:
      1. Manual per-offer override (offer_annotations)
      2. Rule-based matching (regex patterns)
      3. Heuristic fallback (ქეშბექ/cashback + %)
   
   NormalizedBenefit dataclass:
   - benefit_type_id: "CASHBACK_PERCENT"
   - benefit_label_ka: "ქეშბექი"
   - value: 20.0
   - value_unit: "%"
   - rule_id: რომელი წესით მოხდა matching
   - source: "rules", "offer_annotations", "heuristic"


5. build_index.py - ინდექსის აშენება
   ----------------------------------
   
   ერთჯერადი სკრიპტი:
   1. found_offers.json ჩატვირთვა
   2. EmbeddingGenerator ინიციალიზაცია
   3. თითოეული offer-ის preprocessing და embedding
   4. Qdrant კოლექციის შექმნა/განახლება
   5. დოკუმენტების upsert
   
   გაშვება: python -m rag.build_index


6. query_index.py - ინდექსის ტესტირება
   ------------------------------------
   
   Smoke test სკრიპტი:
   - სწრაფი ტესტი RAG pipeline-ის მუშაობის შესამოწმებლად
   
   გაშვება: python -m rag.query_index "თქვენი query"


================================================================================
                        III. LLM INTEGRATION (llm/)
================================================================================

მიზანი: LLM-თან ინტეგრაცია პასუხების გენერაციისთვის

ფაილები:
---------

1. prompts.py - პრომპტების მართვა
   -------------------------------
   
   კლასი: PromptTemplates
   
   a) SYSTEM_PROMPT
      - BOG ასისტენტის როლი
      - პასუხის სტილის ინსტრუქციები:
        * ფაქტობრივი კითხვა → პირდაპირი პასუხი
        * რეკომენდაციის მოთხოვნა → 1-3 ვარიანტი
   
   b) USER_QUERY_TEMPLATE
      - მომხმარებლის query + retrieved offers
   
   c) format_offers(offers) -> str
      - შეთავაზებების ფორმატირება პრომპტისთვის
      - ველები: სარგებელი, ბრენდი, სათაური, კატეგორია, პერიოდი, ქალაქები...
   
   d) create_user_message(query, offers) -> str
      - საბოლოო user message-ის შექმნა


2. chatbot.py - მთავარი ჩათბოტი
   -----------------------------
   
   კლასი: BOGChatbot
   
   მხარდაჭერილი პროვაიდერები:
   - OpenAI (GPT-4, GPT-4o-mini)
   - Google Gemini (gemini-2.0-flash, gemini-1.5-pro)
   - None (მხოლოდ retrieval, LLM-ის გარეშე)
   
   ძირითადი მეთოდები:
   
   a) __init__(retriever, config)
      - Retriever და config ინიციალიზაცია
      - TaxonomyEngine ჩატვირთვა
      - conversation_history ინიციალიზაცია
   
   b) chat(user_message) -> str
      პროცესი:
      1. Provider და model არჩევა (.env-დან)
      2. Retrieval: retriever.retrieve(query)
      3. Taxonomy: თითოეული შედეგის ნორმალიზაცია
      4. Prompt building: system + history + user
      5. LLM call ან formatted output (თუ LLM გამორთულია)
      6. History update
   
   c) reset_conversation()
      - საუბრის ისტორიის გასუფთავება
   
   d) _call_llm(messages, provider, model) -> str
      
      OpenAI:
      - OpenAI SDK v1.x
      - client.chat.completions.create()
      
      Gemini:
      - google.generativeai SDK
      - _resolve_model_name() - ავტომატური მოდელის არჩევა
      - system_instruction + transcript format
   
   ფუნქცია: build_default_chatbot()
   - .env-დან კონფიგურაციის წაკითხვა
   - ყველა კომპონენტის ინიციალიზაცია
   - მზა BOGChatbot-ის დაბრუნება
   
   CLI ინტერფეისი:
   - One-shot: python -m llm.chatbot "query"
   - Interactive: python -m llm.chatbot
   - No-LLM: python -m llm.chatbot --no-llm "query"


================================================================================
                          IV. USER INTERFACE (ui/)
================================================================================

ფაილი: app.py - Streamlit აპლიკაცია
------------------------------------

Streamlit-ზე დაფუძნებული ვებ ინტერფეისი

კომპონენტები:

a) Page Config
   - title: "BOG Offers Chatbot"
   - icon: 💬
   - layout: centered

b) Sidebar
   - "ახალი საუბარი" ღილაკი - ისტორიის გასუფთავება
   - გამოყენების ინსტრუქციები

c) Chat Interface
   - st.chat_message() - მესიჯების ჩვენება
   - st.chat_input() - მომხმარებლის input
   - st.spinner() - "ვეძებ შეთავაზებებს..."

d) Error Handling
   - შეცდომის შემთხვევაში: ლოგირება + user-friendly მესიჯი
   - Troubleshooting hints ქართულად

e) Session State
   - st.session_state.messages - საუბრის ისტორია

f) Bot Caching
   - @st.cache_resource - ბოტის ერთჯერადი ინიციალიზაცია
   - სწრაფი გაშვება შემდგომ requests-ზე


გაშვება: streamlit run ui/app.py
ან: python launch.py


================================================================================
                         V. CONFIGURATION (config.py)
================================================================================

ცენტრალიზებული კონფიგურაციის მართვა

Dataclass-ები:
--------------

1. QdrantConfig
   - url, api_key, collection_name
   - distance, timeout_seconds, upsert_batch_size

2. EmbeddingConfig
   - model_name, normalize, device

3. RetrieverConfig
   - top_k, min_similarity_score, lexical_boost
   - max_results_for_prompt, max_factual_results, min_factual_overlap

4. LLMConfig
   - provider, model, temperature, history_to_keep

5. AppConfig
   - ყველა config-ის გაერთიანება
   - from_env() - .env-დან ჩატვირთვა
   - validate() - ვალიდაცია


================================================================================
                         VI. DATA STRUCTURE (data/)
================================================================================

data/raw/
---------
- found_offers.json - raw scraped data
- cities.json - ქალაქების ID mapping

data/processed/
---------------
- found_offers.json - დამუშავებული მონაცემები (ქალაქებით)
- taxonomy.json - სარგებლის ტიპების ტაქსონომია


Offer JSON სტრუქტურა:
---------------------
{
    "brand_name": "რესტორნის სახელი",
    "title": "შეთავაზების სათაური",
    "category_desc": "რესტორნები და კაფეები",
    "description": "დეტალური აღწერა...",
    "benef_badge": "%",
    "benef_name": "DISCOUNT",
    "product_code": "SOLO",
    "segment_type": "ALL",
    "start_date": "2024-01-01",
    "end_date": "2024-12-31",
    "cities": "თბილისი, ბათუმი",
    "details_url": "https://bog.ge/..."
}


================================================================================
                      VII. ENVIRONMENT VARIABLES (.env)
================================================================================

საჭირო ცვლადები:
----------------

# Qdrant
QDRANT_URL=https://xxx.qdrant.io
QDRANT_API_KEY=your-api-key
COLLECTION_NAME=bog_offers
QDRANT_DISTANCE=COSINE

# Embedding
EMBEDDING_MODEL=BAAI/bge-m3

# Retriever
TOP_K=5
MIN_SIMILARITY_SCORE=0.0
LEXICAL_BOOST=0.15

# LLM
LLM_PROVIDER=gemini  # or openai, auto, none
LLM_MODEL=auto
LLM_TEMPERATURE=0.2
GEMINI_API_KEY=your-gemini-key
OPENAI_API_KEY=your-openai-key


================================================================================
                          VIII. SYSTEM FLOW DIAGRAM
================================================================================

მომხმარებლის Query → Streamlit UI
                          ↓
                     BOGChatbot.chat()
                          ↓
                     OfferRetriever.retrieve()
                          ↓
                     EmbeddingGenerator.generate_embeddings()
                          ↓
                     VectorStore.similarity_search()
                          ↓
                     OfferRetriever.rerank_results()
                          ↓
                     TaxonomyEngine.normalize_offer()
                          ↓
                     PromptTemplates.create_user_message()
                          ↓
                     BOGChatbot._call_llm()
                          ↓
                     LLM Response → მომხმარებელი


================================================================================
                           IX. RUNNING THE PROJECT
================================================================================

1. დამოკიდებულებების დაყენება:
   pip install -r requirements.txt

2. .env ფაილის შექმნა:
   - QDRANT_URL და QDRANT_API_KEY
   - GEMINI_API_KEY ან OPENAI_API_KEY

3. ინდექსის აშენება (ერთჯერადი):
   python -m rag.build_index

4. აპლიკაციის გაშვება:
   python launch.py
   
   ან:
   streamlit run ui/app.py

5. CLI გამოყენება:
   python -m llm.chatbot "თბილისში რესტორნები ქეშბექით"


================================================================================
                              X. KEY FEATURES
================================================================================

✓ მულტილინგვალური ემბედინგები (ქართული მხარდაჭერა)
✓ Qdrant Cloud ვექტორული ბაზა
✓ Hybrid Search (ვექტორული + ლექსიკური)
✓ Query-type aware რანჟირება
✓ Taxonomy-based ნორმალიზაცია
✓ Multi-provider LLM support (OpenAI, Gemini)
✓ Conversation history
✓ Streamlit UI საუბრის ინტერფეისით
✓ CLI ინტერფეისი
✓ Comprehensive logging


================================================================================
                         შექმნილია: 2024-2025
                    Bank of Georgia Offers Assistant
================================================================================
